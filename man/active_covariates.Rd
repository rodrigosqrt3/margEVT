% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R
\name{active_covariates}
\alias{active_covariates}
\title{Extract names of non-seasonal covariates from a fitted model}
\usage{
active_covariates(fit, free_cols = NULL, tol = 1e-04)
}
\arguments{
\item{fit}{An \code{nhpp_fit} object.}

\item{free_cols}{Character vector of column names to always exclude from
the result (intercept and seasonality terms are always excluded).
Default \code{NULL}.}

\item{tol}{Numeric. Coefficients smaller than this in absolute value are
treated as zero (LASSO shrinkage). Default \code{1e-4}.}
}
\value{
Character vector of active covariate names, or \code{NULL} if
  the model has no active non-seasonal covariates.
}
\description{
Returns the covariate names that appear in the model with a non-negligible
coefficient — i.e. those that actually affect the parameters and need to
be supplied during marginalization.
}
