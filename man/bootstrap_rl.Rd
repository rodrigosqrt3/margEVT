% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_rl}
\alias{bootstrap_rl}
\title{Parametric bootstrap confidence intervals for return levels}
\usage{
bootstrap_rl(
  fit,
  data,
  TRs = c(2, 5, 10, 20, 50, 100),
  R = 200L,
  approach = "C",
  marginalize_args = list(),
  level = 0.95,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{fit}{An \code{nhpp_fit} object.}

\item{data}{The data frame used to fit \code{fit}. Must contain column
\code{y} and all covariates.}

\item{TRs}{Numeric vector of return periods. Default \code{c(2,5,10,20,50,100)}.}

\item{R}{Integer. Number of bootstrap replicates. Default \code{200L}.}

\item{approach}{Character. Which marginalization approach to use for each
replicate: \code{"A"}, \code{"B"}, or \code{"C"}. Default \code{"C"}.}

\item{marginalize_args}{Named list of additional arguments passed to
\code{\link{marginalize}} (e.g. \code{n_boot}, \code{seed},
\code{scenarios}, \code{interactions}).}

\item{level}{Numeric. Confidence level. Default \code{0.95}.}

\item{seed}{Integer. Random seed. Default \code{42L}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A data frame with columns \code{TR}, \code{RL_est},
  \code{CI_low}, \code{CI_high}, \code{n_ok} (number of successful
  replicates).
}
\description{
Simulates \code{R} bootstrap samples from the fitted model, refits at the
original penalty and lambda, recomputes return levels via
\code{\link{marginalize}}, and returns empirical quantile CIs.
}
