% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{backtest}
\alias{backtest}
\title{Walk-forward backtesting for NHPP extreme value models}
\usage{
backtest(
  fit,
  data,
  varname,
  TRs = c(2, 5, 10),
  n_obs = 365L,
  window_years = 5L,
  min_train_years = 10L,
  n_boot = 200L,
  interactions = list(),
  year_col = "year",
  min_obs_year = NULL,
  verbose = TRUE
)
}
\arguments{
\item{fit}{An \code{nhpp_fit} object.}

\item{data}{The data frame used to fit \code{fit}. Must contain columns
\code{y}, \code{year} (or \code{year_col}), and all active covariates.}

\item{varname}{Character. Name of the column in \code{data} containing
the raw (non-declustered) series, used to compute observed annual maxima
for validation.}

\item{TRs}{Numeric vector of return periods to evaluate. Default
\code{c(2, 5, 10)}.}

\item{n_obs}{Integer. Observations per year. Default \code{365L}.}

\item{window_years}{Integer. Width of each validation window in years.
Default \code{5L}.}

\item{min_train_years}{Integer. Minimum number of training years before
the first validation window. Default \code{10L}.}

\item{n_boot}{Integer. Bootstrap years for covariate marginalization
within each window. Default \code{200L}.}

\item{interactions}{Named list of interactions passed to
\code{\link{build_cov_annual}}. Default \code{list()}.}

\item{year_col}{Character. Name of the year column. Default \code{"year"}.}

\item{min_obs_year}{Integer. Minimum observations in a validation year
for it to be included. Default \code{as.integer(n_obs * 0.8)}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A list with three elements:
  \item{results}{Data frame with one row per validation year, containing
    the observed annual maximum, PIT value, and exceedance indicators for
    each return period.}
  \item{binom_tests}{Data frame with binomial test results for each TR.}
  \item{ks_test}{Data frame with KS test result for PIT calibration,
    or \code{NULL} if fewer than 5 validation years had events.}
}
\description{
Evaluates predictive performance of a fitted \code{nhpp_fit} model via
walk-forward cross-validation. For each window, the model is refit on
training data at the original penalty and lambda, return levels are
computed, and exceedance rates are compared to nominal rates via
binomial tests. Probabilistic calibration is assessed via a
Kolmogorov-Smirnov test on the PIT values.
}
